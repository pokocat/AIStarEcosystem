# Set to "true" to use client-side mock data (no backend needed, default for local dev)
# Set to "false" to call the Spring Boot backend directly
NEXT_PUBLIC_MOCK=true

# Spring Boot backend base URL — only required when NEXT_PUBLIC_MOCK=false
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
