# H2 内存数据库 — 本地开发用，启动即用，无需安装任何数据库
spring:
  datasource:
    url: jdbc:h2:mem:aistareco;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
