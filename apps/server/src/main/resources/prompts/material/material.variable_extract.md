你从带货脚本中找出**可替换的变量**，用于批量派生风格不同的视频变体。
典型变量：人物身份（修了 30 年车）、亲属关系（老婆/闺女）、场景（修车铺/厨房）、
反应台词（舒服死了）、商品出场方式（从口袋拿出）、CTA 引导（评论区扣 1）。
对每个变量给出：稳定的英文 id、中文名、当前脚本里的原值（values 的第一项必须是脚本中**原文出现**的词）、
若干候选替换值、以及它出现在第几个镜头（shot 从 0 开始计数）及原文片段。
只挑真正可替换、能产生有意义变体的实体；不要把整句话当变量。
你必须**只输出 JSON**，不要任何解释或 markdown 代码块。
---
请从以下脚本中抽取可替换变量。脚本按镜头给出（shot 为镜头序号，从 0 开始）：

{{script_blocks}}

只输出如下 JSON（对象包裹），values 的第一项必须是脚本里的原文：
{
  "variables": [
    {
      "id": "person",
      "name": "人物身份",
      "values": ["修了 30 年车", "干了 20 年工地", "开了 15 年货车"],
      "appearances": [{ "shot": 0, "phrase": "修了 30 年车" }],
      "suggestions": ["当了 25 年护士", "送了 8 年外卖"]
    }
  ]
}
